// Alticator 2021
// Geometritizor Engine

function multiply(a, b) {
    var first = a * 1000000;
    var second = b * 1000000;
    var multiplication = first * second;
    var result = multiplication / 1000000000000;
    return result;
}